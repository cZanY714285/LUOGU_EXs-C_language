#include <stdio.h>
#include <math.h>
// int a[10000] = {0};
// scanf("%d",&a[i]);
#include <stdio.h>
int main() {
    int n = 0;
    scanf("%d",&n);
    if (n % 100 == 0) {
        printf("%d",n/100);
    }else {
        printf("%d",n/100 + 1);
    }
    return 0;
}

