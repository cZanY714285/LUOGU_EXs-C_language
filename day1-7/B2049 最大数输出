#include <stdio.h>
#include <math.h>
#include <limits.h>

int main() {
    int max = INT_MIN;
    long long a[5];
    for (int i = 0; i < 3; i++) {
        scanf("%lld",&a[i]);
        if (max < a[i]) {
            max = a[i];
        }
    }printf("%d",max);
    return 0;
}
