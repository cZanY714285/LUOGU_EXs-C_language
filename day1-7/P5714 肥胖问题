#include <stdio.h>
int main(){
        double m,h,bmi = 0;
        scanf("%lf %lf",&m,&h);
        bmi = m / h / h;
        if (bmi < 18.5) {
                printf("Underweight");
        }else if (bmi < 24) {
                printf("Normal");
        }else {
                printf("%.4f\n",bmi);
                printf("Overweight");
        }

        return 0;
}
