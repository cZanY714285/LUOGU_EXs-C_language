#include <stdio.h>
#include <math.h>
// int a[10000] = {0};
// scanf("%d",&a[i]);
int main() {
    int a,b = 0;
    scanf("%d %d",&a,&b);
    if (a < 10 && b < 20) {
        printf("0");
    }else {
        printf("1");
    }
    return 0;
}
